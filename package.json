{
  "name": "ai-job-search-bot",
  "version": "1.0.0",
  "description": "Automated AI-powered job search and application bot",
  "main": "api/server.js",
  "scripts": {
    "dev": "concurrently \"npm run api:dev\" \"npm run dashboard:dev\" \"npm run n8n:dev\"",
    "api:dev": "cd api && npm run dev",
    "dashboard:dev": "cd dashboard && npm start",
    "n8n:dev": "n8n start",
    "install:all": "npm install && cd api && npm install && cd ../dashboard && npm install",
    "db:setup": "cd api && npm run db:migrate",
    "test": "jest",
    "test:integration": "jest --config jest.integration.config.js",
    "test:e2e": "cypress run",
    "build": "cd dashboard && npm run build && cd ../api && npm run build",
    "start": "cd api && npm start",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "keywords": [
    "job-search",
    "automation",
    "ai",
    "web-scraping",
    "n8n",
    "puppeteer"
  ],
  "author": "AI Job Search Bot Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "jest": "^29.5.0",
    "cypress": "^12.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}